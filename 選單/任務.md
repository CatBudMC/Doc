# 任務
`T` 遊戲幣、`A` 遊戲幣的獲取來源，每日、每週、每月會固定刷新。奉獻 `XXX` 表示要把該物品丟入岩漿或仙人掌。

## 每日
共 7 個。
- 打招呼：`20 T` ＋ `40` 經驗值 ＋ `1 A`
- 每天完成：`40 T` ＋ `1 A`
- 奉獻鑽石：`10 T` ＋ `20` 經驗值 ＋ `1 A`
- 登入簽到：`20 T` ＋ `40` 經驗值 ＋ `1 A`
- 奉獻獄髓：`60 T` ＋ `120` 經驗值 ＋ `1 A`
- 輕鬆挖掘：`20 T` ＋ `40` 經驗值 ＋ `1 A`
- 殭屍獵手：`20 T` ＋ `40` 經驗值 ＋ `1 A`

## 每週
共 8 個。
- 釣魚專家：`40 T` ＋ `80` 經驗值 ＋ `2 A`
- 繁殖專家：`40 T` ＋ `80` 經驗值 ＋ `2 A`
- 每週完成：`100 T` ＋ `1 A`
- 每天都做：`100 T` ＋ `200` 經驗值 ＋ `1 A`
- 飲食專家：`40 T` ＋ `80` 經驗值 ＋ `2 A`
- 挖掘專家：`40 T` ＋ `80` 經驗值 ＋ `2 A`
- 斬擊專家：`40 T` ＋ `80` 經驗值 ＋ `2 A`
- 交易專家：`40 T` ＋ `80` 經驗值 ＋ `2 A`

## 每月
共 5 個。
- 月簽到表：`200 T` ＋ `4 A`
- 血鑽石：`200 T` ＋ `4 A`
- 獵龍人：`200 T` ＋ `4 A`
- 隨機探索者：`200 T` ＋ `4 A`
- 出生地探索者：`200 T` ＋ `4 A`

## 成就
共 28 個。
- 釣魚大師：`1,000 T` ＋ `2,000` 經驗值
- 釣魚至尊：`2,000 T` ＋ `4,000` 經驗值
- 釣魚天尊：`4,000 T` ＋ `8,000` 經驗值
- 釣魚天神：`8,000 T` ＋ `16,000` 經驗值
- 繁殖大師：`1,000 T` ＋ `2,000` 經驗值
- 繁殖至尊：`2,000 T` ＋ `4,000` 經驗值
- 繁殖天尊：`4,000 T` ＋ `8,000` 經驗值
- 繁殖天神：`8,000 T` ＋ `16,000` 經驗值
- 簽到大師：`1,000 T` ＋ `2,000` 經驗值
- 簽到至尊：`2,000 T` ＋ `4,000` 經驗值
- 簽到天尊：`4,000 T` ＋ `8,000` 經驗值
- 簽到天神：`8,000 T` ＋ `16,000` 經驗值
- 飲食大師：`1,000 T` ＋ `2,000` 經驗值
- 飲食至尊：`2,000 T` ＋ `4,000` 經驗值
- 飲食天尊：`4,000 T` ＋ `8,000` 經驗值
- 飲食天神：`8,000 T` ＋ `16,000` 經驗值
- 挖掘大師：`1,000 T` ＋ `2,000` 經驗值
- 挖掘至尊：`2,000 T` ＋ `4,000` 經驗值
- 挖掘天尊：`4,000 T` ＋ `8,000` 經驗值
- 挖掘天神：`8,000 T` ＋ `16,000` 經驗值
- 斬擊大師：`1,000 T` ＋ `2,000` 經驗值
- 斬擊至尊：`2,000 T` ＋ `4,000` 經驗值
- 斬擊天尊：`4,000 T` ＋ `8,000` 經驗值
- 斬擊天神：`8,000 T` ＋ `16,000` 經驗值
- 交易大師：`1,000 T` ＋ `2,000` 經驗值
- 交易至尊：`2,000 T` ＋ `4,000` 經驗值
- 交易天尊：`4,000 T` ＋ `8,000` 經驗值
- 交易天神：`8,000 T` ＋ `16,000` 經驗值
