# 頻道
每個頻道中的訊息都是獨立的，建立頻道的人擁有編輯頻道和踢出成員的權限，只有加入了該頻道的玩家才能看到聊天內容。每個頻道在 Discord 上都有對應的語音同步頻道，頻道會同步聊天訊息在遊戲和 Discord 中。頻道的聊天顏色為淺藍色。當玩家進入或離開頻道時，會顯示 `XXX 已進入頻道 YYY` 或 `XXX 已離開頻道 YYY`。在頻道中的玩家想要發送全域訊息，需要在訊息開頭加上 `!` 符號，例如 `!大家早安～`。。
